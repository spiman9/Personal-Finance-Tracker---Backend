spring.application.name=PersonalFinanceTracker
# required for postgresql connection or any connection to the database
spring.datasource.url=jdbc:postgresql://localhost:5432/FinanceDatabase
spring.datasource.username=postgres
spring.datasource.password=Pritham09

# JPA / Hibernate configurations
# update the database schema on application startup
spring.jpa.hibernate.ddl-auto=update 
#show sql statements in the console
spring.jpa.show-sql=true
#format sql statements in the console
spring.jpa.properties.hibernate.format_sql=true